import styled from 'styled-components';

export const Badge = styled.button`
  background: none;
  border: 0;
  position: relative;
`;

export const Box = styled.button`
  background: none;
  border: 0;
  position: relative;
  margin-bottom: 10px;
  margin-left: 15px;
`;
